<?xml version='1.0'?>

<!--
This operational configuration override file is set up for use with Coherence in
a development mode.
-->
<coherence>
  <cluster-config>
  
    <member-identity>
      <!--
      Note: All members of the cluster must specify the same cluster name in
      order to be allowed to join the cluster.
      -->
      <cluster-name system-property="tangosol.coherence.cluster">InfoskyCacheClusterDev</cluster-name>
      <member-name  system-property="tangosol.coherence.member">ctas-node</member-name>
      <role-name    system-property="tangosol.coherence.role">Infosky Cache Role</role-name>
    </member-identity>

    
    <multicast-listener>
      <address system-property="tangosol.coherence.clusteraddress">224.3.5.3</address>
      <port system-property="tangosol.coherence.clusterport">35101</port>
      <time-to-live system-property="tangosol.coherence.ttl">4</time-to-live>
      <join-timeout-milliseconds>10000</join-timeout-milliseconds>
    </multicast-listener>
    
	<services>
		<service id="3">
			<init-params>
				<init-param id="4">
					<param-name>local-storage</param-name>
					<param-value system-property="tangosol.coherence.distributed.localstorage">false</param-value>
				</init-param>
			</init-params>
		</service>
	</services>    
  </cluster-config>
  
  <logging-config>
    <severity-level system-property="tangosol.coherence.log.level">4</severity-level>
    <character-limit system-property="tangosol.coherence.log.limit">0</character-limit>
  </logging-config>
  
  <configurable-cache-factory-config>
    <class-name>com.tangosol.net.DefaultConfigurableCacheFactory</class-name>
    <init-params>
      <init-param>
        <param-type>java.lang.String</param-type>
        <param-value system-property="tangosol.coherence.cacheconfig">coherence-cache-config.xml</param-value>
      </init-param>
    </init-params>
  </configurable-cache-factory-config>
</coherence>
